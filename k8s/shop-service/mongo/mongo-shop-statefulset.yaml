apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo-shop
spec:
  serviceName: mongo-shop
  replicas: 1
  selector:
    matchLabels:
      app: mongo-shop
  template:
    metadata:
      labels:
        app: mongo-shop
    spec:
      containers:
        - name: mongo-shop
          image: mongo:latest
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              value: "admin"
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: "password"
            - name: MONGO_INITDB_DATABASE
              value: shop
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-shop-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: mongo-shop-data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 512Mi
