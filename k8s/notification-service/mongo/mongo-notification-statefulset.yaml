apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo-notification
spec:
  serviceName: mongo-notification-svc
  replicas: 1
  selector:
    matchLabels:
      app: mongo-notification
  template:
    metadata:
      labels:
        app: mongo-notification
    spec:
      containers:
        - name: mongo-notification
          image: mongo:latest
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              value: "admin"
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: "password"
            - name: MONGO_INITDB_DATABASE
              value: notification
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: mongo-notification-data
              mountPath: /data/db
  volumeClaimTemplates:
    - metadata:
        name: mongo-notification-data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 512Mi
